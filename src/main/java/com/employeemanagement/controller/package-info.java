/**
 * REST API controllers — expose HTTP endpoints documented in Swagger UI.
 *
 * <p>All endpoints except {@code /api/auth/**} and Swagger paths require a JWT token:
 * {@code Authorization: Bearer &lt;token&gt;}</p>
 */
package com.employeemanagement.controller;
